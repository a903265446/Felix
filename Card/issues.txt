1. 有的地方用U，有的地方不用
2. 枚举有的有值，有的没有值
3. SDMMC_SetHostClock定义
4. ADMA fill 的方式，两个描述符表示一次传输需要修改。
5. ADMA SDHC_GetAdmaEntryNumber 有没有必要提供这个API
6. SDMMC_SendCommand对于超过最大SDHC支持数据块的处理
if (req->data->blockCount > SDHC_MAX_BLOCK_COUNT)
        {
            cmdConfig.dataBlockSize  = req->data->blockSize;
            cmdConfig.dataBlockCount = SDHC_MAX_BLOCK_COUNT;
            flags &= ~SDHC_ENABLE_BLOCK_COUNT;
        }
7. SDHC_SetEnableCmd 是否要分开
8. assert host 语句缺少
9. 指针*位置统一
10. 专用CD pin检测卡是否插入
 枚举括号位置统一
多个blocks读写检测信号忙不能一直忙等
while(!(SDHC_GetPresentState(base) & SDHC_DATA0_LINE_LEVEL)){}
#define CARD_DATA_ERR_AUTO_CMD12 位置

11. R1 error检测， data flag config， 继续传送数据的方式
//SD_Switch 64 bytes
//SD_SendScr 8 bytes
//SD_Read
SD_Write 

GetDefaultDataConfig
DataConfig
BlockSendCommand(cmd, cmdData)
......

checkResponseError

WaitDataTransferComplete(cmdData)


